import '../services/base_service.dart';

class ApiProvider {
  // Placeholder for providing access to API services
  // Can be integrated with GetX, Provider, or Riverpod

  final BaseService _baseService;

  ApiProvider(this._baseService);

  // Expose specific data fetching methods
}
