import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFE8523A); // Coral
  static const secondary = Color(0xFF7B5EA7); // Plum
  static const background = Color(0xFFFAF7F2); // Beige chaud
  static const foreground = Color(0xFF1A1A1A); // Charcoal
  static const card = Color(0xFFFFFFFF); // Blanc
  static const border = Color(0xFFE8E4DE); // Bordure douce
  static const mutedForeground = Color(0xFF9CA3AF); // Gris texte secondaire
  static const green = Color(0xFF16A34A); // Vert positif
  static const red = Color(0xFFEF4444); // Rouge négatif
  static const amber = Color(0xFFF59E0B); // Catégorie 3
  static const slate = Color(0xFF94A3B8); // Catégorie 4

  static const shadowSm = BoxShadow(
    color: Color(0x0A000000),
    blurRadius: 8,
    offset: Offset(0, 2),
  );
}

